import React from 'react';
import ReactDOM from 'react-dom/client';
import QrCodeComponent from './QrComponent';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <QrCodeComponent/>
  </React.StrictMode>
);


